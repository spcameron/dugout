version: "2"

sql:
  - engine: postgresql
    schema:
      - "database/migrations/*.sql"
    queries:
      - "database/queries/*.sql"
    gen:
      go:
        package: "database"
        out: "internal/database"
        sql_package: "pgx/v5"
        emit_json_tags: true
